{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(formSousCategorieProduit) }}

    {{ form_row(formSousCategorieProduit.nom, {'label' : 'Nom du produit :','attr' : {'placeholder' : "Nom"}}) }}
    {{ form_row(formSousCategorieProduit.CategorieProduit, {'label' : 'utilisateur:','attr' : {'placeholder' :
        "utilisateur"}}) }}
    <button type="submit" class="btn btn-success ">
        {% if editMode %}
            Enregistrer les modifications
        {% else %}
            Ajouter la sous catégorie
        {% endif %}
    </button>

    {{ form(formSousCategorieProduit) }}
{% endblock %}