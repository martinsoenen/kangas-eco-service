{% extends 'base.html.twig' %}

{% block title %} Devis en ligne - {{ parent()}}
{% endblock %}
{% block body %}
<div class="objects">
    {{ form_start(form) }}
    <div class="row">
        {{ form(form) }}
    </div>
</div>
    {{ form_end(form) }}
{% endblock body %}

{% block javascripts %}
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    <script>
        $('select#categorie_collecte_nom.form-control').change(function() {

             let value = $('select#categorie_collecte_nom.form-control :selected').text();
            console.log(value);
             $.ajax(
                 url : '/entreprise/devis?categorie='+ value,
                 type : 'GET',
                 dataType: "integer",
                 success: function(response) {
                     console.log(response);}
            })
      });
   </script>
{#    <script>#}
{#        function ajout(listCateg) {#}
{#            var existe = 0;#}
{#            if(listCateg.options[listCateg.selectcategorie_collecte_nom.form-control].value !=0){#}
{#                for(i=1 ; i< document.getElementById('categorie').length ; i++){#}
{#                    if(document.getElementById('categorie').options[i].text == listCateg.options[listCateg.selectedIndex].text){#}
{#                        existe = 1;#}
{#                    }#}
{#                    else{#}
{#                        if()#}
{#                    }#}
{#                }#}
{#            }#}

{#        }#}
{#    </script>#}

{% endblock %}