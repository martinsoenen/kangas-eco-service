{% extends 'base.html.twig' %}

{% block title %}Devis en ligne - {{ parent() }}{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{form_row(form.entreprise)}}
        {{form_row(form.nom)}}
        {{form_row(form.email)}}
        {{form_row(form.produits)}}
        {{form_row(form.rue)}}
        {{form_row(form.cp)}}
        {{form_row(form.ville)}}
        {{form_row(form.date)}}
        {{form_row(form.commentaire)}}
    {{ form_end(form) }}
{% endblock %}
